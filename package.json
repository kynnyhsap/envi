{
  "name": "envi-workspace",
  "version": "0.0.0",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "catalog": {
      "@1password/sdk": "0.5.0",
      "@effect/platform-node": "4.0.0-rc.116",
      "@effect/vitest": "4.0.0-rc.116",
      "@types/node": "26.6.2",
      "effect": "4.0.0-rc.116",
      "typescript": "7.0.2",
      "vitest": "5.0.1"
    }
  },
  "type": "module",
  "scripts": {
    "dev": "bun --conditions=@envi/source packages/envi/src/bin.ts",
    "dev:node": "node --conditions=@envi/source packages/envi/src/bin.ts",
    "build": "bun run --filter '*' build",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "typecheck": "tsc --noEmit && tsc --noEmit -p examples/tsconfig.json",
    "check": "bun fmt:check && bun lint && bun typecheck",
    "test:unit": "vitest run --project unit",
    "test:e2e": "bun run build && vitest run --project e2e",
    "test:onepassword": "bun run build && node --env-file-if-exists=.env.local node_modules/vitest/vitest.mjs run --project onepassword --no-file-parallelism",
    "test:unit:bun": "bun --bun vitest run --project unit",
    "test": "bun test:unit && bun test:unit:bun && bun test:e2e",
    "verify": "bun run --parallel check test",
    "fixture:onepassword": "bun packages/onepassword/scripts/fixture.ts"
  },
  "devDependencies": {
    "@1password/sdk": "catalog:",
    "@effect/platform-node": "catalog:",
    "@effect/vitest": "catalog:",
    "@envi/1password": "workspace:*",
    "@envi/core": "workspace:*",
    "@oxlint/plugins": "1.83.0",
    "@types/node": "catalog:",
    "effect": "catalog:",
    "envi": "workspace:*",
    "oxfmt": "0.68.0",
    "oxlint": "1.83.0",
    "oxlint-tsgolint": "7.0.2002",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "engines": {
    "bun": ">=1.3",
    "node": ">=22.19.0"
  },
  "packageManager": "bun@1.4.2"
}
